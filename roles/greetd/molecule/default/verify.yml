---

- name: Verify
  hosts: all
  gather_facts: no

  tasks:
    - name: Check if greetd was successfully installed
      ansible.builtin.command:
        greetd -h
      register: out
      changed_when: no
      failed_when:
        - out.rc != 0
