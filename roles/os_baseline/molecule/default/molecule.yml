---

dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: instance
    image: ghcr.io/karras/archlinux-ansible-testing:latest
    pre_build_image: true

provisioner:
  name: ansible

verifier:
  name: ansible
