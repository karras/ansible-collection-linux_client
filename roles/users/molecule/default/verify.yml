---

- name: Verify
  hosts: all
  gather_facts: no

  tasks:
  - name: check if /home/clark exists
    ansible.builtin.stat:
      path: /home/clark
    register: this
    failed_when:
      - not this.stat.exists
