---

- name: Converge
  hosts: all
  gather_facts: true

  tasks:
    - name: Include role firewalld
      ansible.builtin.include_role:
        name: firewalld
      vars:
        firewalld_default_zone: drop
        firewalld_services:
          - ssh
